# Client (React SPA)

This is the React client for the portfolio website.

## Development

1. Install dependencies:
   npm install
2. Start dev server:
   npm run dev

The app preserves the existing static UI and behavior by rendering the legacy single-page markup and loading the existing section scripts.
